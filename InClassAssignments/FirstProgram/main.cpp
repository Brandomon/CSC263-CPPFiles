#include "MyPrototypes.h"

int main()
{
    int x, y;

    //GetInput(x, y);

    //PrintInfo(x, y);

    srand(time(0));

    //Sometimes you need to cast to an unsigned int if it's giving errors
    //srand((unsigned int)(time(0)));

    //RandomNumbers();

    FormatExample();

    return 0;
}
